# 电磁感应 fixture 演示

这是从前期“高中物理电磁场”讨论中保留的最小可复现样例，用于展示课堂疑点如何流向课后复习，以及教师如何创建可审核的多模态素材任务。

运行：

```powershell
pnpm demo
```

演示依次执行 8 步：读取教师确认的课程卡、记录匿名疑点、准备引导式复习、创建家庭 Agent 衔接快照、生成匿名班级洞察、草拟教学计划、创建视频任务接口、进入教师审核门。

它不会访问网络、不会调用真实模型、不会连接真实学生账号，也不会执行 ComfyUI 或 3D 生产任务。fixture 中没有可向学生泄露的答案字段。

